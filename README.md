# Chinese-Text-Clustering

 
## 一、目的
 
- 理解文本聚类的基本概念与意义；
- 掌握经典文本聚类算法（如K-Means、层次聚类）的原理与实现方法；
- 用特定指标（如轮廓系数）定量评估聚类结果。
 
## 二、任务
 
- 预处理教师提供的文本数据集（包含分词、词嵌入等步骤）；
- 选择多种文本聚类算法（如K-Means、层次聚类），实现文本聚类；
- 输出聚类结果与评估结果，记录程序运行情况（评估可调用sklearn等库的函数）；

